{% block menu %}
<header class="navigation">
	<div class="container">
		<div class="row">
			<div class="col-lg-12">
				<nav class="navbar navbar-expand-lg p-0">
					<a class="navbar-brand" href="{{ path('home')}}">
						<img src="{{asset("images/logo.png")}}" alt="Logo">
					</a>

					<button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarsExample09" aria-controls="navbarsExample09" aria-expanded="false" aria-label="Toggle navigation">
						<span class="ion-android-menu"></span>
					</button>

					<div class="collapse navbar-collapse ml-auto" id="navbarsExample09">
						<ul class="navbar-nav ml-auto">
							<li class="nav-item active">
								<a class="nav-link" href="{{ path('home')}}">Home</a>
							</li>
							<li class="nav-item dropdown">
          					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Article</a>
          					<ul class="dropdown-menu">
            					<li><a class="dropdown-item" href="{{ path('article')}}">Voir Article</a></li>
            					<li><a class="dropdown-item" href="{{ path('app_article_new')}}">Creer Article</a></li>
          					</ul>
        					</li>
							<li class="nav-item"><a class="nav-link" href="{{ path('app_utilisateur_index')}}">Utilisateur</a></li>
							<li class="nav-item dropdown">
								<a class="nav-link dropdown-toggle"  href="{{ path('app_categorie_index')}}" role="button" data-bs-toggle="dropdown" aria-expanded="false">Categorie</a>
								<ul class="dropdown-menu">
									{{ render(controller('App\\Controller\\CategorieController::recentCategories')) }}
								</ul>
							</li>
							<li class="nav-item"><a class="nav-link" href="{{ path('app_avis_index')}}">Avis</a></li>
							{% if is_granted('IS_AUTHENTICATED_FULLY') %}
								{% if is_granted("ROLE_ADMIN") %}
									<li class="nav-item"><a class="nav-link" href="{{ path('admin')}}">admin dashboard</a></li>
									<li class="nav-item"><a class="nav-link" href="{{ path('app_logout')}}">Se déconnecter</a></li>
								{% else %}
									<li class="nav-item"><a class="nav-link" href="{{ path('app_logout')}}">Se déconnecter</a></li>
								{% endif %}
							{% else %}
							<li class="nav-item"><a class="nav-link" href="{{ path('app_login')}}">login</a></li>
							<li class="nav-item"><a class="nav-link" href="{{ path('app_register')}}">register</a></li>
							{% endif %}
							{{ render(controller('App\\Controller\\ArticleController::recherche')) }}
						</ul>
					</div>
				</nav>
			</div>
		</div>
	</div>
</header><!-- header close -->
{% endblock %}